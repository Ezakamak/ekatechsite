import { resolveDisplayName } from '../../../../_offFriends';
import { ensureOffSeasonEngineSchema } from '../../../../_offSeasons';
export async function onRequestGet(context:any){await ensureOffSeasonEngineSchema(context); const u=new URL(context.request.url); const gameKey=(u.searchParams.get('gameKey')||'').trim(); const limit=Math.min(100,Math.max(1,Number(u.searchParams.get('limit')||50))); const id=Number(context.params.id||0); const gameFilter = gameKey === 'tech_duel' ? 's.tech_duel_matches > 0' : gameKey === 'cipher_break' ? 's.cipher_break_matches > 0' : gameKey === 'core_clash' ? 's.core_clash_matches > 0' : '1=1'; const rows=await context.env.DB.prepare(`SELECT s.user_id,s.total_points,s.total_matches,s.wins,s.losses,s.draws,s.win_rate,s.tech_duel_matches,s.cipher_break_matches,s.core_clash_matches,u.name,u.email,u.nickname,op.display_name off_display_name,op.avatar_url,op.avatar_data FROM off_leaderboard_snapshots s JOIN users u ON u.id=s.user_id LEFT JOIN off_profiles op ON op.user_id=u.id WHERE s.season_id=? AND ${gameFilter} ORDER BY s.total_points DESC,s.win_rate DESC,s.total_matches DESC,s.user_id ASC LIMIT ?`).bind(id,limit).all<any>(); return Response.json({ok:true,leaderboard:(rows.results||[]).map((r:any,i:number)=>({rank:i+1,userId:r.user_id,displayName:resolveDisplayName(r),avatarUrl:r.avatar_data||r.avatar_url||null,totalPoints:r.total_points,totalMatches:r.total_matches,wins:r.wins,losses:r.losses,draws:r.draws,winRate:r.win_rate}))});}

import { useEffect, useState } from "react";
import { EmptyState, OffPanel, Pill } from "./offUi";
const metrics = ["season_xp", "total_xp", "level", "matches", "wins"];
export function OffLeaderboard() { const [metric, setMetric] = useState("level"); const [data, setData] = useState<any>({ leaders: [] }); useEffect(() => { fetch(`/api/off-leaderboard?metric=${metric}`, { credentials: "same-origin" }).then(r => r.json()).then(setData); }, [metric]); return <OffPanel title="Leaderboard" eyebrow="Season / All Time / Games"><div className="mb-5 flex gap-2 overflow-x-auto pb-2">{metrics.map(m => <button key={m} onClick={() => setMetric(m)} className={`shrink-0 rounded-full border px-4 py-2 text-sm ${metric === m ? "border-cyan-300/30 bg-cyan-300/15 text-cyan-100" : "border-white/10 bg-white/[0.04] text-white/55"}`}>{m}</button>)}</div>{data.me && <div className="mb-4 rounded-2xl border border-purple-300/20 bg-purple-300/10 p-4 text-white">Senin sıran: <strong>#{data.me.rank}</strong> · {data.me.value}</div>}<div className="grid gap-3">{(data.leaders || []).map((r: any, i: number) => <div key={`${r.user_id}-${i}`} className="flex items-center justify-between gap-3 rounded-2xl border border-white/10 bg-black/25 p-4"><div className="flex min-w-0 items-center gap-3"><Pill tone={i < 3 ? "amber" : "white"}>#{i + 1}</Pill><span className="truncate font-semibold text-white">{r.name}</span></div><span className="font-semibold text-cyan-100">{r.value}</span></div>)}{!(data.leaders || []).length && <EmptyState>Sıralama verisi yok.</EmptyState>}</div></OffPanel>; }
